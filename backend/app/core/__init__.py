"""基础设施层（core）：与具体业务无关的通用支撑。

职责：
- storage：统一 JSON 持久化仓库（仓储模式），消除各业务模块重复的读写/加锁/原子写实现；
- config：配置中心（路径解析 + 分级配置加载：环境变量 > 运行时产物 > 配置文件 > 默认值）；
- netutil：网络工具（UA / TTL 缓存 / 带编码回退的文本与 JSON 拉取）。

本层不依赖任何业务模块，供 domain / application / api 各层复用。
"""
