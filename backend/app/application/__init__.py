"""应用服务层（application）：业务用例编排的门面（Facade）。

职责：把「一个业务用例涉及多个领域/实现模块」的编排逻辑收拢为可复用服务，
API 路由只做 HTTP 参数校验与响应，不直接触碰引擎/存储等实现细节。

- simulation_service：核心仿真域（预置模型/解析/仿真/扫描/审计/参数元数据）；
- strategy_service：  策略库用例（CRUD / 应用 / 标记）；
- optimizer_service： AI 优化模型用例（上下文同步 / 训练控制 / 版本管理 / 应用）。

服务对象为模块级单例（无状态或轻状态），供 api 层注入使用，也便于测试直接构造。
"""
