# 2026-08-12 同步到服务器

## 同步流程（沿用 rsync）
- 本地 → `root@172.19.134.45:/root/qzb/jianpai/`，SSH 正常。
- 本轮改动：ProcessList.vue（去掉行内编辑/定位按钮，上一轮已 build）。dist 已最新（无源文件比 dist 新）。
- 传输：72 文件 / 13.5MB（含重新生成的 dist 资源哈希）。

## ⚠️ 发现并修复的 P0：后端崩溃
- 重启容器后服务起不来：`backend/app/report.py` line 405 `SyntaxError: unterminated string literal`，导致 main.py 导入链 `from .report import generate_report` 失败，整个 uvicorn 起不来（首页/health 全空）。
- 根因：`report.py` 之前从未同步到服务器（首次上传即带坏语法）——一个 f-string 内嵌套跨行条件表达式 + 隐式字符串连接，引号未闭合。
- 修复：把三目条件提取为独立的 `strategy_part` 变量（普通 f-string + 条件表达式），主 prompt 用 `{strategy_part}` 插值。已 `py_compile` + `compileall` 全包通过。
- 教训：每次同步后端后**必须 `docker compose restart steel-twin` 并验 /api/health**，不能只看 rsync 成功。

## 同步时清理的垃圾 + 排除项更新
- 发现一批本地调试产物被 rsync 传上服务器：根目录 `chrome_long*.png/html`、`chrome_longfix*.png`、`diag_scene.png`、`void_industrial.png`、`night-port.png`、`.playwright-cli/` 目录（playwright 调试）、`generated-images/`、`outputs/`。已在服务器 rm + 加入 rsync 排除项：
  `chrome_*.png` `chrome_*.html` `diag_scene.png` `void_industrial.png` `night-port.png` `.playwright-cli/` `generated-images/` `outputs/`
- 最终服务器与本地 0 差异（checksum 重跑：0 传输 0 删除）。

## 验证结果（全部通过）
- 容器 Up；启动日志无报错；`/api/health` → 200 `{"status":"ok"}`。
- 首页资源哈希 `index-CDKb5gzE.js` / `index-BT3isKQM.css`，与本地 dist 一致。
- `param-schema`：sinter_plant/caster 等均含 `electricity`（之前新增参数生效）。
- `/api/simulate`：6 工序，高炉 13891 tCO₂/h（与历史基线一致）。
- 容器日志显示 WatchFiles 已 reload report.py 无 SyntaxError。

访问：http://172.19.134.45:40014
